[project]
name = "zo-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.7.1",
    "transformers[torch]>=4.53.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.1",
]
